const { expect } = require("chai")
const hre = require("hardhat")

describe("My First Test", () => {
    // let token, oceantToken, owner, add1, add2, totalCap = 100000000, tokenBlockReward = 50;
    // beforeEach()
})